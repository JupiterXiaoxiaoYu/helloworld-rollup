apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloworld-rollup.fullname" . }}
  labels:
    {{- include "helloworld-rollup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helloworld-rollup.selectorLabels" . | nindent 4 }}
